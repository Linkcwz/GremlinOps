# Codex bootstrap (agent-specific overrides)

Codex reads the repo-root [`AGENTS.md`](../AGENTS.md) directly; this folder
holds Codex-only overrides.

## Codex-specific overrides

None at this time. Codex follows the canonical `AGENTS.md` exactly, the same
as every other agent.

The sibling folders [`Claude/`](../Claude/) and [`Gemini/`](../Gemini/)
mirror this one. Durable knowledge never lives here — see the shared tree.
