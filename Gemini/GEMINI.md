# Gemini bootstrap (agent-specific overrides)

Gemini loads the repo-root [`GEMINI.md`](../GEMINI.md), which points at the
canonical [`AGENTS.md`](../AGENTS.md); this folder holds Gemini-only
overrides.

## Gemini-specific overrides

None at this time. Gemini follows the canonical `AGENTS.md` exactly, the
same as every other agent.

The sibling folders [`Codex/`](../Codex/) and [`Claude/`](../Claude/) mirror
this one. Durable knowledge never lives here — see the shared tree.
