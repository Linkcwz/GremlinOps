# Secret NAMES only — committed so agents know what exists and what it is
# for. Values live in the encrypted vault / authorized local stores.
#
# NAME                          PURPOSE
# GIT_MIRROR_TOKEN              push token for the public mirror remote
# DNS_API_TOKEN                 read-only zone token for DNS checks
# MEDIA_STACK_API_KEY           service API key for the media stack
# SSO_ADMIN_BOOTSTRAP           break-glass SSO admin credential (vault only)
